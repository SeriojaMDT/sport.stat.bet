name: Build BestSportStats V2.3 APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Apply BestSportStats V2.2 source
        run: python tools/apply_v21_source.py
      - name: Apply BestSportStats V2.3 patch
        run: python -c "import base64,zlib; exec(zlib.decompress(base64.b64decode(open('v23-patch.b64z').read())))"
      - name: Set V2.3 version
        run: |
          sed -i "s/versionCode [0-9][0-9]*/versionCode 9/" app/build.gradle
          sed -i "s/versionName '[^']*'/versionName '2.3-test'/" app/build.gradle
      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Android SDK
        uses: android-actions/setup-android@v3
      - name: Install SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace
      - name: Upload V2.3 source
        uses: actions/upload-artifact@v4
        with:
          name: bestsportstats-v23-source
          path: app/src/main/java/com/serghei/footballpredictions/MainActivity.java
          if-no-files-found: error
      - name: Upload V2.3 APK
        uses: actions/upload-artifact@v4
        with:
          name: bestsportstats-v23-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
