plugins {
    id 'com.android.application' version '8.8.2' apply false
}

// Goals Stats v1.4 sources and version are prepared by the GitHub Actions
// patch steps before Gradle starts. Keep the root build file neutral so the
// app can use the dynamic Blogger INDEX instead of a hard-coded URL list.
